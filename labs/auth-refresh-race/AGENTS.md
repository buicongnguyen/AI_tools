# Lab instructions for Codex

- Treat `src/auth-client.js` as the candidate implementation.
- Do not copy `solution/auth-client.js` during the exercise; it is for verification after completion.
- Run `npm run reproduce` before editing and preserve its raw failing output.
- Do not change `src/fake-auth-server.js` or weaken tests to make the candidate pass.
- Preserve refresh-token rotation and never log access or refresh token values.
- Completion requires `npm run reproduce` to pass after your fix and a fresh diff review.
